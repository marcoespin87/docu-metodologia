{
  "name": "caso1-sistema-pedidos",
  "version": "1.0.0",
  "private": true,
  "description": "Caso 1 (desde cero) — metodologia SDD spec-anchored",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
